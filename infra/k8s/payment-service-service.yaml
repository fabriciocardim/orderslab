apiVersion: v1
kind: Service
metadata:
  labels:
    app: payment-service
  name: payment-service
spec:
  type: LoadBalancer
  ports:
    - name: "8082"
      port: 8082
      targetPort: 8080
  selector:
    app: payment-service

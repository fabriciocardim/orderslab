apiVersion: v1
kind: Service
metadata:
  labels:
    app: postgres-api
  name: postgres-api
  namespace: orderslab
spec:
  type: LoadBalancer
  ports:
    - name: "5432"
      port: 5432
      targetPort: 5432
  selector:
    app: postgres-api
